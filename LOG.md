# Log

- 2026-08-21 — Tagged `geer-phase`; stripped src/notebooks/reports/scripts/docs; new CLAUDE.md, README, LOG.
- 2026-08-21 — data/ reorganized to mirror layer groups: reference/, inspection_grid/, imagery/ (was basemaps/); orphan GEER data removed (at tag geer-phase); 39 layers repointed in QGIS.
- 2026-08-21 — manifest.yaml regenerated from the live project (163 layers, 16 sources); old entries for removed layers dropped.
- 2026-08-22 — Added qgis/after_filter.py: toolbar "Filter AFTER to view" / "Show all AFTER" (Private-flag spatial filter via imagery_footprints); openProject macro set in memory (Lorne: enable macros, save). Replaces the ZONE viewer groups once validated.
- 2026-08-22 — Removed `viewer - AFTER imagery` (42 pointer groups) and `ZONE index — AFTER imagery` from the project (in memory; registry bridge disabled during removal). data/imagery/imagery_zones.geojson left on disk for Lorne to delete.
- 2026-08-22 — Inspection grid rework: dropped `show` (both tables), added `status`/`status_change_date`/`note` to grid_1km — NOTE: MCP field tools wrote these to the gpkg directly, not staged. New qgis/inspection_grid.py: cached grid_progress() expression function + 10 km virtual fields (n_reviewed, n_unreviewable, pct_done); relation, status styles, progress labels, form defaults set in memory; macro loads both modules.
- 2026-08-31 — 1 km grid: backfill status=unreviewed staged in edit session (Lorne commits); inspection_grid.py now stamps status_change_date at commit for real status changes (apply-on-update default removed — it only fires for form edits); NULL→unreviewed transitions do not bump the date.
- 2026-08-31 — Added 5 PlanetScope 2026-08-01 scenes as XYZ stream layers (tiles.planet.com, API key in layer URL) to AFTER stream group, in memory; scenes cover La Guaira–Caracas–Guarenas coast, cloud 1–19%. No entries yet in imagery_footprints.geojson.
- 2026-08-31 — Fixed the 5 Planet stream layers: QGIS 3.44 XYZ provider does not decode %3F/%3D in the url param, so the api_key query must stay raw in the uri; sources rewritten in place, rendering verified (La Guaira coast).
- 2026-08-31 — 5 Planet 08-01 footprints staged in an edit session on new project layer "Imagery footprints" (data/imagery/imagery_footprints.geojson; Lorne commits — direct disk write blocked/avoided per data/ rule); after_filter.footprints() now prefers the in-project layer so filtering works pre-commit.
- 2026-08-31 — GEGD Pro (pro.gegd.com) API key verified against /streaming/v1/ogc/wms (maxar_api_key param); test layer added then removed at Lorne’s request — key not yet stored anywhere in the project.
- 2026-08-31 — Added qgis/keys.yaml (gitignored) holding Planet + GEGD Pro API keys for scripts.
- 2026-08-31 — QGIS crashed adding inline GDAL_WMS layer; after restart: re-staged 6 footprints (5 Planet + BlackSky, edit session open), re-added BlackSky 06-25 0.91m GL16 scene as GDAL_WMS layer via gitignored qgis/streams/gegd_72397b6d.xml (CQL featureId filter — QGIS WMS provider drops CQL_FILTER, GDAL passes it), verified on canvas.
- 2026-08-31 — Added 54 GEGD Pro scenes (of 62 matching post-06-24, ≤1 m, AOI: 33 BlackSky, 17 WV-2/3, 3 WV-1 pan, 1 GE-1; 7 skipped as duplicates of Vantor streams, 1 already added) as GDAL_WMS layers via gitignored qgis/streams/*.xml; 54 footprints staged in the open footprints edit session; spatial filter verified.
- 2026-08-31 — Stream group: removed junk sub-group1/2 nests (stream + external drive), flattened and sorted all 125 stream layers by date-name (registry bridge disabled during restructure).
- 2026-09-01 — External imagery drive moved: /Volumes/One Touch/satellite_imagery → /Volumes/PortableSSD/satellite_imagery. Updated manifest.yaml delivery + 68 PS paths, absolute tile paths in 7 maxar_external VRTs, and repointed 63 PS layers in QGIS in-memory (project not saved).
- 2026-09-01 — Restructured AFTER imagery: merged (stream) + (external drive) groups into single flat sorted "AFTER — post-event imagery"; renamed 63 PS layers to the MM-DD · gsd · sensor · location · id convention with footprint-derived locations (suffix "(Planet drive)") and retagged 5 Maxar visuals "(Maxar local)"; footprints layer_name/location/gsd/label updated (edit session open, uncommitted); manifest names updated; after_filter.py now targets the merged group and auto-hides layers on unmounted /Volumes drives. Project not saved.
- 2026-09-01 — TODO: 3 AFTER NASA-stream layers have no footprint feature, so "Filter AFTER to view" falls back to their full regional extent and always shows them: satellogic aoi-coast mosaic, skysat aoi-coast mosaic, wv-2 caracas 212947. Fix: probe exportImage alpha on a tile grid (small ~20 km bboxes only — wide bboxes 504 even when healthy; ImageServer catalog /query is broken, so per-component layers are not possible), vectorize strips, add footprint features. Blocked mid-attempt: all of gis.earthdata.nasa.gov went down (nginx 404 site-wide) — retry when host is back. Probe tile list at scratchpad tiles.txt (regenerable: 11x3 grid over the satellogic extent).
- 2026-09-01 — GEGD Pro catalog checked for the 5 raw Maxar collects (142038/212928/212947/140331/140407): none present — CSDA tasked orders, not streamable via GEGD; only NASA Earthdata streams 212947 (+ maybe la-guaira in their coastal mosaic — verify when host is back). Added scripts/rebuild_maxar_visuals.py: clean rebuild of all 6 visuals (incl. never-built puerto-cabello 203718) — per-order 2-98 pctl stretch sampling, histogram-aligns 142038 recovered tiles via VRT scale/offset, JPEG q92 + overviews, outputs .new.tif for QC-then-swap. Parse + sampler smoke-tested; heavy run is Lorne's.
- 2026-09-02 — CLAUDE.md rules loosened: Claude may make additive/reversible disk changes (new files, .new.tif outputs, manifest/doc edits) and run such scripts itself; destructive actions (overwrite/delete existing data, save project, commit) remain Lorne's.
- 2026-09-02 — Rebuilt all 6 Maxar visuals as .new.tif via scripts/rebuild_maxar_visuals.py (2-98 pctl stretch, JPEG q92, overviews; incl. first-ever puerto-cabello 203718). 142038 first pass had a QC-caught dark seam: percentile-based cross-order normalization was biased (~0.42x) by clouds in the 4 recovered-tile sample; rebuilt with edge-median ratios (~1.06x, measured R2C03|R2C04) — seam gone (raw orders differ only ~6%). Awaiting Lorne QC + swap; old .tifs untouched. Footprints edit session committed (63 renamed entries now on disk).
- 2026-09-02 — Swapped the 6 rebuilt Maxar visuals into place (old .tifs, holey.bak, stale aux deleted per Lorne); re-added the 5 layers + new puerto-cabello 203718 layer sorted in the AFTER group (in-memory — save to persist). Manifest: 203718 entry added, recipe notes now point at scripts/rebuild_maxar_visuals.py.
- 2026-09-02 — Added puerto-cabello 203718 footprint feature (strip polygon derived from raster valid-data mask, largest part, simplified 50 m) to imagery_footprints.geojson; committed.
- 2026-09-02 — Mask regeneration completed in place for all 6 Maxar visuals: two-stage build (lossless interm → gdalwarp -dstalpha → JPEG q92 + internal mask, NoData unset). Two bugs caught by QC en route: gdalbuildvrt -addAlpha marks coverage not nodata (mask was all-valid), and low stretch minima mapped raw 0 to 1 breaking the nodata test on dark scenes (212947/212928 blue) — fixed with a src_min floor of src_max/500 (script updated). Full-res mask verification per scene: 10.7-28.3% exterior masked, edges clean, no value-based transparency left. Layers re-added in QGIS (in-memory — save to persist).
